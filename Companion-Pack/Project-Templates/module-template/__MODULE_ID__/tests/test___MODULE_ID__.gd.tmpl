extends RefCounted

func run() -> bool:
	var repository := InMemory__MODULE_CLASS__Repository.new()
	var service := __MODULE_CLASS__Service.new(repository)
	return (
		service.register(&"first")
		and not service.register(&"first")
		and not service.register(&"")
	)
