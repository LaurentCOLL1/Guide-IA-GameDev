# Exploitation

Construire l'index depuis `corpus/`, rechercher, supprimer un identifiant puis vérifier avec `verify-deleted`. Pour une suppression source définitive, retirer le fichier JSON et reconstruire : le résultat doit être identique à l'index élagué.
