name: Livre V Chapter 26 Finalizer Trigger

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The temporary runner on main owns finalization."

# trigger: finalize Livre V chapter 26
# created-at: 2026-07-30T01:18:00+02:00
# retry-after-link-and-audit-fix
