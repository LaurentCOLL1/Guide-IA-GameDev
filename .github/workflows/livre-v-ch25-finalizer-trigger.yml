name: Livre V Chapter 25 Finalizer Trigger

on:
  pull_request:
    branches:
      - main
    types:
      - synchronize

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The temporary runner on main owns finalization."
