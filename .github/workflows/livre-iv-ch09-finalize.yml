name: Livre IV Chapter 9 Finalizer

on:
  pull_request:
    branches: [main]
    paths:
      - '.qa/finalize-livre-iv-ch09.py'
      - '.qa/ch09-chapter.part*.zlib.b64'
      - '.qa/ch09-chapter.part02*.b64'
      - '.qa/ch09-audit.zlib.b64'
      - '.qa/ch09-proof.zlib.b64'
      - '.github/workflows/livre-iv-ch09-finalize.yml'

permissions:
  contents: write

jobs:
  finalize-and-validate:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout chapter branch
        uses: actions/checkout@v4
        with:
          ref: docs/livre-iv-chapitre-09-chargements-streaming
          fetch-depth: 1

      - name: Detect pending finalizer
        id: pending
        shell: bash
        run: |
          if test -f .qa/finalize-livre-iv-ch09.py; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install lightweight dependencies
        if: steps.pending.outputs.run == 'true'
        run: python -m pip install --disable-pip-version-check pyyaml

      - name: Materialize chapter lot
        id: materialize
        if: steps.pending.outputs.run == 'true'
        continue-on-error: true
        shell: bash
        run: |
          python .qa/finalize-livre-iv-ch09.py > .qa/ch09-finalizer-diagnostic.txt 2>&1
          code=$?
          cat .qa/ch09-finalizer-diagnostic.txt
          exit $code

      - name: Upload finalizer diagnostic
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: livre-iv-ch09-finalizer-diagnostic
          path: .qa/ch09-finalizer-diagnostic.txt
          if-no-files-found: error

      - name: Stop after materialization failure
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'failure'
        run: exit 1

      - name: Assert reader boundaries
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        shell: bash
        run: |
          chapter="Livre-IV/CHAPITRE-09-Chargements-streaming-et-gestion-des-ressources.md"
          ! grep -qi 'GPT-5.6 Sol' "$chapter"
          ! grep -qi 'Prochaine action' "$chapter"
          ! grep -qi 'PDF' "$chapter"

      - name: Validate structure metadata links and duplicates
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        run: python tools/validate_chapters.py

      - name: Validate structured explanations
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        run: python tools/check_code_explanation_structure.py

      - name: Audit usage markers
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        run: python tools/check_context_markers.py --check

      - name: Audit semantic marker consistency
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        run: python tools/audit_contextes_semantiques.py --check

      - name: Measure chapter context coverage
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        shell: bash
        run: |
          python - <<'PY'
          import re
          from pathlib import Path

          path = Path("Livre-IV/CHAPITRE-09-Chargements-streaming-et-gestion-des-ressources.md")
          lines = path.read_text(encoding="utf-8").splitlines()
          marker = re.compile(r"^> \*\*\[(PS|CMD|WSL|DCT|DCK|VSC|WEB|APP|SORTIE|LECTURE)\]")
          inside = False
          openings = 0
          errors = []
          for index, line in enumerate(lines):
              match = re.match(r"^```([^`]*)$", line.strip())
              if not match:
                  continue
              if not inside:
                  openings += 1
                  cursor = index - 1
                  while cursor >= 0 and not lines[cursor].strip():
                      cursor -= 1
                  if cursor < 0 or not marker.match(lines[cursor].strip()):
                      errors.append(index + 1)
                  inside = True
              else:
                  inside = False
          if inside:
              raise SystemExit("Bloc Markdown non fermé")
          if openings != 54:
              raise SystemExit(f"Nombre de blocs inattendu: {openings}")
          if errors:
              raise SystemExit(f"Blocs sans repère: {errors}")
          print({"chapter": path.as_posix(), "controlled_blocks": openings, "errors": 0})
          PY

      - name: Assert lightweight validation produced no PDF
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        shell: bash
        run: |
          if find . -type f -iname '*.pdf' -newer .qa/finalize-livre-iv-ch09.py -print -quit | grep -q .; then
            echo "Un PDF a été produit pendant la validation légère."
            exit 1
          fi

      - name: Upload QA evidence
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: livre-iv-ch09-validation-without-pdf
          path: |
            Livre-IV/QA/AUDIT-CHAPITRE-09.md
            Livre-IV/QA/VALIDATION-FINALE-CHAPITRE-09.yaml
          if-no-files-found: error

      - name: Commit finalized chapter lot
        if: steps.pending.outputs.run == 'true' && steps.materialize.outcome == 'success'
        shell: bash
        run: |
          rm .qa/finalize-livre-iv-ch09.py
          rm .qa/ch09-chapter.part01.zlib.b64
          rm .qa/ch09-chapter.part02.zlib.b64
          rm .qa/ch09-chapter.part02a.b64
          rm .qa/ch09-chapter.part02b.b64
          rm .qa/ch09-chapter.part03.zlib.b64
          rm .qa/ch09-chapter.part04.zlib.b64
          rm .qa/ch09-chapter.part05.zlib.b64
          rm .qa/ch09-audit.zlib.b64
          rm .qa/ch09-proof.zlib.b64
          rm -f .qa/ch09-finalizer-diagnostic.txt
          rm .github/workflows/livre-iv-ch09-finalize.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "docs(livre-iv): créer le chapitre 9 sur les chargements et le streaming"
          git push origin HEAD:docs/livre-iv-chapitre-09-chargements-streaming
