name: Patch Chapter 9 Round 2

on:
  push:
    branches:
      - docs/livre-ii-ch09-saves-versioning
    paths:
      - .github/workflows/patch-ch09-round2.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: docs/livre-ii-ch09-saves-versioning
          fetch-depth: 0

      - name: Apply reviewed safety corrections
        run: python tools/patch_ch09_round2.py

      - name: Commit corrected chapter and remove temporary tooling
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs(livre-ii): harden chapter 9 load safety [skip ci]"
          git push origin HEAD:docs/livre-ii-ch09-saves-versioning
